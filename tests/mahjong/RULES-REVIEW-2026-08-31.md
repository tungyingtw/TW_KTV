# F1／F2／F3 修正後覆核

> V1–V4 已整合至 `RULES-FIX-EXECUTION-2026-08-31.md` 的一次性修復工作包與 T01–T18 驗收。以工程書為唯一實作入口，本文件保留修正前反例與完成狀態；不要另起平行修正批次。

日期：2026-08-31。本文件保留歷次覆核。最新為下列第四次驗收；本次僅執行唯讀測試並更新驗收文件，未修改遊戲程式、持久化測試或重新建置，未 commit／push。

## 結論

第四次驗收的 D1–D3 已修正：電腦空牌山吃碰入口會拒絕、吃牌 UI 回呼綁定當次反應，C1／C3 測試已改為可辨識對應防護。完整 T01–T18 證據與瀏覽器／實機操作尚未完成，不能標記本批全面結案。

以封存《台灣16張休閒規則AI執行工程書》及本批工程書為判準。不將固定門風、無尾墩、休閒花牌加成、基本胡 1 台、單一倍率收付或單贏家順序當作未經授權的新玩法修改。

## 第四次驗收：剩餘缺口與測試盲點

以下 D1／D2 原先在來源與現有 dist、16／13 張模式重現；修正後已在兩種模式重跑來源，並在正式 dist 重跑 D1／D2。這是入口／回呼重放驗收，不代表一般玩家點擊必然遇到，亦不是新增計台金額錯誤。

### D1：電腦空牌山吃碰入口仍接受（P2，原 C1／T08）

- 位置：`src/computer-actions.js:15`、`:33`、`:81`。共用反應資格不檢查牌山，只有明槓另外檢查 `wall.length`。
- 重現：電腦 1 有兩張一萬或二三萬，玩家正式打出最後一萬，牌山為空。直接呼叫正式碰／吃入口仍移牌、建立副露並交給電腦 1 出牌；16／13 張暗手分別變成 14／11。
- 正常 `handleDiscardClaims` 會攔末張副露，不能因此宣稱動作入口也有同等防護。相同牌組在非空牌山的吃碰正例均成功；拒絕修正不得破壞正例。
- 修正結果：共用電腦棄牌反應資格加入牌山非空檢查；D1 在 13／16 張確認空牌山吃碰均拒絕且無副作用，非空正例保留。

### D2：舊吃牌回呼會操作新局當前選組（P2，原 C2／T06）

- 位置：`src/render-view.js:164`。閉包只保留選組 `index`，未封存牌局或反應識別；動作再從目前 `pendingClaim` 取選組，當前 token 自然相符。
- 重現：保留正式渲染的一二萬吃三萬按鈕回呼；另建 `runId=2` 的合法四五萬吃六萬反應。重放舊回呼仍吃入六萬，副露成為四五六萬。兩模式、來源及 dist 均重現，無需新局重用同一牌 ID。
- 修正結果：吃、明槓 UI 回呼封存 `runId`、棄牌 token 與牌 ID；玩家入口核對當前待反應。D2 確認舊局吃牌回呼無法消耗新局棄牌。

### D3：新增 C1／C3 測試會假通過（P2，原 C3／V3）

- 位置：`rules-fix.test.js:191`、`:235`。C1 未像正式 `offerPlayerClaim` 設定 `kind="discard"`，即使移除 `canPong` 防護也會被 kind 擋住；C3 一副碰配置 16／13 張，舊 `% 3 === 2` 已會拒絕，不能測出「多三張仍可槓」的回歸。
- 唯讀變異驗證：分別在 VM 記憶體移除 `canPong` 防護、把自身槓 gate 改回 modulo，原測試仍全部通過。未將變異寫回檔案。
- 在記憶體補正 kind、把 C3 改成一副碰加 17／14 張後，現行程式通過，移除防護的版本失敗。這證實既有修正有效，但持久化測試未保護它們。
- 修正結果：C1 使用正式 `kind="discard"`，C3 使用一副碰後多三張的 17／14 張反例。第四次唯讀時的修正夾具能辨識原對應防護移除；後續另加入非空牌山 gate，因此現在 C1 同時受牌山與權限保護，不能單以此例證明個別 `canPong` 條件的變異覆蓋。

### 本次證據與未完成

- 重跑 `acceptance`、`flow-recheck`、`rules-fix`：36／36、0 跳過；計分腳本 14 cases 通過。
- `npm run build` 成功；正式 dist 重跑 D1／D2 為 2／2 通過。
- 提交前再查：來源／dist 共 52／52 通過，涵蓋兩模式的合法玩家／電腦吃碰明槓、空牌山電腦副露拒絕，以及同局／跨局舊吃與明槓 UI 回呼拒絕。使用正式牌山、四家正確暗手、正式棄牌入口及 UI 回呼，前後核對 144 張實體牌及唯一 ID；未寫入持久化測試。使用者已授權 commit 供線上驗證，push 留給使用者。
- 尚未完成：既定 T01–T18 整合證據、正式輸出瀏覽器及實體手機驗收。本次沒有新增瀏覽器證據；本文件下方第三次結果僅保留歷史，以本節為準。

## 第二次驗收：尚未完成的既定防護

以下三個入口反例均在 `public/games/mahjong` 及現有 `dist/games/mahjong` 執行，各覆蓋 `tw16`／`simple13`，共 12 次重現。從正式牌山取唯一 ID，四家填入正確暗手張數；整局 144 張實體牌均有歸屬，沒有重複 ID。只隔離畫面／排序等副作用，使用正式動作模組。這些是直接呼叫或刻意重放失效回呼的測試，未證明一般玩家當次點擊必定觸發。

### C1：末張胡／過仍可從碰牌入口執行碰（P2）

- 位置：`src/player-actions.js:38`、`:109`。
- 原因：新增入口驗證刪掉原有 `claim.canPong` 檢查，也未檢查牌山仍有牌。`state.pendingClaim === claim` 是從當前 pendingClaim 取出後自比，不能代表這個動作獲准。
- 合法 16 張暗手：萬 `1123`、筒 `123456`、條 `123`、東 `111`；13 張改用筒 `123`。牌山空、上家最後打出一萬，正式 `createPlayerDiscardClaim(..., true)` 得到 `canWin=true`、`canPong=false`。
- 結果：呼叫正式 `claimPong()` 仍接受，玩家暗手由 16／13 變成 14／11，建立碰並消耗最後棄牌。正常畫面會隱藏碰鈕，但入口不符合 T07／T08 的禁止副露要求。
- 修正命令：恢復反應權限、事件類型、非空牌山及正確待摸張數檢查；讓玩家與電腦的吃碰明槓入口使用一致資格。在驗證失敗前後比對全部狀態與副作用。

### C2：過期候選仍能作用於新局或較後反應（P2）

- 位置：`src/render-view.js:180`、`src/player-actions.js:95`、`src/computer-actions.js:81`。
- 跨局反例：以萬 `111123456789`、筒 `12568`（13 張模式為筒 `12`）產生正式暗槓 UI 回呼，另建使用相同 ID 的新局，將 `runId` 由 1 改為 2，再呼叫保留的舊回呼。兩模式均接受，新局暗手由 17／14 變 13／10 並新增暗槓。現有 `scheduleAction` 的 runId 防護並不涵蓋這個按鈕閉包。
- 較早反應反例：電腦 2 的最後棄牌為一萬，之後電腦 3 已打出九萬；讓電腦 1 的正式碰入口重用前面的一萬。來源牌仍是電腦 2 自己牌河末張，所以入口接受，移除舊棄牌、建立副露並將 current 改為 1，較新的九萬仍留在歷史末端。
- 修正命令：把候選與 UI 回呼綁定產生時的 `runId`、待打階段／反應識別；執行時核對目前全桌反應，不只看某家牌河末張。補回 T06 與同局跨反應案例，保留合法連續槓。

### C3：張數防護與新增測試仍不足（P2）

- 位置：`src/state-flow.js:9`、`src/player-actions.js:95`、`src/computer-actions.js:67`、`tests/mahjong/rules-fix.test.js:164`。
- `canDiscardNow` 仍只檢查 `length % 3 === 2`；新槓入口沿用此 helper，未驗證 `winningTileCount - 3 × 非花副露數`。有一副碰時合法摸後暗手應為 14／11，但 T18 補槓測試配置成 17／14，多了三張，入口仍接受。
- T17 只用 `pop()` 模擬出牌後張數，沒有執行正式出牌、補槓反例、450ms 交接及下一家僅摸一次。T18 以手動 `wall.pop()` 補牌，且其餘三家暗手仍空；未覆蓋 CPU 入口、錯誤來源或合法連續槓。
- T01 的電腦只有兩張且以替身決定／執行碰；快取例仍是玩家與另一家同碰同種牌的不可能組合。T07 宣稱所有玩家槓入口但只呼叫暗槓；末張流局例仍以空手與假的 endRound 驗證。
- 修正命令：依既定 T01–T18 修正夾具與正式流程整合測試；先檢查四家暗手、副露、實體牌守恆，再執行動作。不要只追加測試名稱或修改通過數。

### 驗收證據與本輪界線

- 既有測試重跑為 31／31、0 跳過；計分腳本 14 cases。上述 12 次反例是本輪唯讀 Node／VM 檢查，尚未加入持久化回歸。
- 核對上輪工具紀錄後，所謂「正式輸出瀏覽器驗收」實際使用 `npm run dev`，不是 dist。844×390 已進局及開關牌河／規則；1440×900 僅核對大廳控制項。捲動、長結算、896×414 與 dist 瀏覽器操作仍未驗證。
- 本輪不新增玩法、不修改既定台數或收付公式，不將入口缺口誤報為已發現新的計台金額錯誤。將 C1–C3 留在原 V1–V3 工作包修復，再驗收並結案；不得只等實體手機測試就標記完成。

以下 V1–V4 與第三次結果保留歷史；與上文衝突時，以第四次驗收為準。

## 第三次實作回報（第四次已更正完成範圍）

- C1：玩家碰與明槓入口恢復 `canPong`／`canKong` 檢查；玩家、電腦吃碰明槓同時核對當次活躍棄牌。牌山空的胡／過反應不能建立副露。
- C2：每張正式棄牌建立 `runId`、token、出牌者與牌 ID；玩家待反應與電腦入口都核對。槓牌按鈕在產生時封存 `runId`，舊局按鈕不能取得新局識別。無人反應時，在換下一家前清除活躍棄牌。
- C3：用 `winningTileCount - 3 × 非補花副露數` 計算可出牌的暗手張數；已有一副碰時，16 張為 14、13 張為 11。T18 的補槓夾具已改為正確張數。
- 驗證：`acceptance`、`flow-recheck`、`rules-fix` 共 34／34 通過；計分 14 cases 通過；`npm run build` 成功，生成正式模組已包含上述防護。未 commit／push。

## V1：出牌後仍可按槓並取得補牌（P1）

- 位置：`index.html:4080`、`src/render-view.js:169`、`src/player-actions.js:58`、`index.html:4315`。
- 原因：`discardSelected()` 打完牌仍保留 `current === 0`，等待 450ms 後交給下一家。出牌時重繪槓牌選項，但顯示與動作入口只檢查玩家索引、待反應與牌山，未檢查是否仍處於可出牌階段。
- 16 張反例：玩家摸牌後持有萬 `111123456789`、筒 `12568`，打出九萬且無人反應。此時暗手 16 張、`canDiscardNow=false`，正式 `renderKongOptions()` 仍產生暗槓按鈕。執行該按鈕綁定的正式處理函式後，暗手變成 13 張、建立一副暗槓、牌山由 79 變 78；原本 450ms 的下一家排程仍存在。
- 13 張反例：萬 `111123456789`、筒 `12`，同樣打九萬後暗手 13 張，仍可按槓變成 10 張並消耗一張牌山。
- 兩例均從正式牌山取唯一實體牌，為其餘三家填入對應張數，執行正式出牌、反應及補牌入口。測試以無人吃碰的可控決策隔離 AI，畫面／音效／排程使用替身；未宣稱已完成真實瀏覽器點擊。
- 影響：玩家可在出牌完成後多做槓牌動作，改變手牌及下一家原本會摸到的牌。不是單純殘留按鈕。
- 修正結果：已在玩家與電腦動作入口、槓牌選項顯示共用合法待打判斷；T17 以 13／16 張的出牌交接情境直接呼叫正式入口，確認沒有補牌、副露或狀態變更。

## V2：舊槓選項可重複建立副露（P2）

- 位置：`src/player-actions.js:58`、`:66`，相關 `state-flow.js` 的取牌與副露寫入。
- 反例：從合法 17／14 張手牌取得一萬四張的暗槓選項，完成一次暗槓及補牌後，已沒有合法暗槓候選，但同一舊選項仍可再次傳入正式 `claimConcealedKong()` 並補牌。兩次都接受，副露變 2，同四張實體牌 ID 出現兩次；16 張模式暗手由合法 14 變 15 張，13 張模式由 11 變 12 張，兩者都失去正常出牌節奏。只補上手牌 `% 3` 檢查不能擋住第二次呼叫，必須重新核對選項。
- 原因：移牌函式只濾除存在的 ID，入口不核對選項內的牌仍在暗手，也不核對補槓仍對應原碰牌。這是失效回呼的入口反例，未證明普通雙擊在真實瀏覽器一定會送出兩次。
- 電腦明槓正式 wrapper／模組也沒有空牌山或失效候選的直接入口檢查；正常反應分支雖已阻擋末張明槓，仍未滿足工程書要求的入口防護。
- 修正結果：正式入口會從當前手牌、副露、來源牌河重新驗證候選；wrapper 收到拒絕即停止。T18 已驗證暗槓與補槓成功補牌後重放舊候選會拒絕且狀態不變。

## V3：必驗項目沒有完整實作成測試（P2）

- `rules-fix.test.js:75` 的 T01 將電腦手牌設成兩張且替換整個 AI 決策；沒有保存工程書要求的合法 13／16 張、正式困難 AI 反例。本輪命令列另驗證兩模式的正式困難 AI 均確實選擇電腦 1 碰，尚未持久化成整合測試。
- `rules-fix.test.js:99` 的候選快取測試在玩家持有兩張三萬時，仍用替身回傳另一電腦碰三萬；未配置其合法手牌，不能作為可發生的全局反例。依 T03 改用玩家可碰、下家電腦可吃，或其他合法候選組合。
- `rules-fix.test.js:121` 宣稱測所有玩家槓入口，但只呼叫暗槓；只用 16 張模式，而且實際暗手只有四張。
- `rules-fix.test.js:133` 的末張測試四家暗手皆空、`endRound` 是替身，不能驗證有人可吃碰時被阻擋，也不能證明只結算一次、代幣不變或本場只加一次。
- T02、T03、T06、T09、T11 等沒有相應的完整持久化流程案例；T04／T05／T07／T08／T10 尚未達表定覆蓋，T12 的既有末張補花流程集中在 16 張。
- 修正命令：按原 T01–T16 逐項標示真實證據與未完成部分；補入合法實體牌、正式規則／動作／結算模組、正反例及重複操作檢查。不得靠測試名稱或總通過數宣稱所有條件已覆蓋。

## V4：玩家可見桌規仍有漏項（P2）

- 位置：`index.html:3743` 的結算段落與 `public/games/mahjong/README.md`。
- 尚未完整交代單一倍率公式與「底加每台金額」的差別、不採連 N 拉 N、七搶一／八仙各為基本胡 1 加特殊 8／16 台再依付款方加莊家／本場、普通補花自摸不另算槓上開花。
- 現有文案「兩者只計基本胡與特殊花胡」沒有接續說明莊家／本場仍適用，可能讓玩家對實際金額產生落差。
- 修正結果：規則抽屜與 README 已揭露固定收付公式、莊家／本場適用條件、七搶一／八仙台數與普通補花不算槓上開花；尚待實機確認長內容可捲動及可關閉。

## 本輪已驗證

1. 既有驗收／流程／本批測試：31／31 通過，0 跳過；計分腳本亦通過（輸出 14 cases）。通過數不代表 T01–T16 已全數覆蓋。
2. 收付擴展試算：9,216 組，涵蓋 13／16 張、底注 50／100／200、全部贏家與莊家、合法放槍者／自摸、本場 0／1／2／20、餘額 0／1／49／100／199／5000。以已知 4 台手牌測 `stake × (T + D + D × N)`、實扣下限及實際變化，全部吻合；未據此宣稱每一台型組合均正確。
3. 一般胡牌結構：以不同於正式遞迴拆牌的方法，按各花色逐位消耗刻子／順子需求，對照 11,000 手牌。涵蓋 13／16 張、所有合法副露組數，包含 5,518 個可胡樣本；正式判定與獨立判定一致，5,716 個返回拆法均保留原手牌張數與種類。固定 seed 為 8312026，全部從最多四張同種牌的實體牌山取樣。
4. 核對目前計分表、合法拆法取最高分、互斥台型、普通／特殊花胡、付款方、輪莊與本場流程。未重現新的純金額算式錯誤；未全排列所有合法牌型與操作時序。
5. T17／T18 已新增為持久化測試；瀏覽器／實體手機尚未操作。`npm run build` 已重新成功執行，不以此取代正式操作回歸。

## 收尾與停止條件

繼續原 F1／F2／F3 批次，禁止重做已通過 A／B 批或增加新玩法。

1. 在原工作包修復第二次驗收的 C1／C2 入口缺口，依 C3 補齊 T01–T18 的實際驗收，更新每項證據。
2. 重新建置，於正式輸出重跑反例並完成桌面及手機橫向操作回歸後才標記本批完成；提交與 push 遵循使用者後續指示。
