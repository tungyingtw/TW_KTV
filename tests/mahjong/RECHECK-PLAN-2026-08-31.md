# 二次驗收與定案修正計畫

## 結論與範圍

2026-08-31 二次驗收初驗曾未通過。修正後原有 18 組測試、既有 14 組測試及新增 5 組正式流程案例均通過，正式建置通過。桌面與實體手機操作回歸尚未執行。

本次已修正正式程式、補測與更新狀態。使用者已授權先 commit，再由使用者 push 後直接線上驗證；不要代為 push，也不要將提交視為實機驗收通過。下列 R1/R2/R3 為已完成歷史紀錄；不重新執行整份舊 A/B 工程書，不同時改寫共用主檔，不追加玩法。

## 已完成，禁止重做

- 保留逐筆收付公式、384 種收付矩陣、實扣金額與同日存檔行為。
- 保留暗槓門清、三暗刻反例修正、平胡限制及互斥台型。
- 保留手動搶槓事件修正、特殊花胡獨立計分與末張花牌登記。
- 保留玩家零收付的中性結算顯示。
- 保留 16 張主模式、13 張練習、既定台數與固定單贏家規則。

## 已完成修正與證據

### R1：補花無牌可補，玩家卡在不能出牌的回合（P1）

位置：`public/games/mahjong/index.html` 的 `beginTurn()`（3989 行）、`supplementAfterKong()`（4320 行）；相關 `src/flower-flow.js`、`src/turn-flow.js`。

初驗反例：

- 玩家原有 16 張暗手，摸到牌山最後一張花。花牌登記完成後暗手仍為 16 張，`running=true`，提示卻是「請選一張牌打出」，`canDiscardNow=false`，沒有流局結果。
- 玩家暗槓後有 13 張暗手，補到最後一張花，仍停留在 13 張且沒有流局結果。
- 八仙過海控制例通過：持有六花、手牌花七、牌山花八，完成登記後確實胡牌。保留此行為。

已完成：

1. 補花結果已回傳缺少補牌狀態；特殊花胡優先於流局。
2. 玩家一般摸花與槓後補花的末張反例已通過；八仙過海控制例仍通過。
3. 13 / 16 模式與所有操作分支的人工回歸仍保留為未完成項目。

### R2：電腦補槓漏查另外兩名電腦的搶槓胡（P1）

位置：`public/games/mahjong/index.html` 的 `offerPlayerRobKong()`（4221 行）、`completeComputerAddedKong()`（4234 行）、`passClaim()`（4344 行）。

初驗反例：電腦 1 要補槓一萬，玩家不能胡，電腦 2 能胡。規則模組 `findRobKongWinner()` 正確回傳 2，但正式補槓流程直接把碰升級成補槓，未進入搶槓結算。玩家對搶槓選過後也會進入同一個直接補槓入口。

已完成：

1. 電腦補槓前已檢查其他電腦；玩家對搶槓按過後也會繼續檢查。
2. 電腦搶槓時保留原碰牌、以 `robKong` 進入單次結算，不再補槓或補牌。
3. 玩家可胡、無人可胡、完整 13 / 16 場景與實際代幣顯示仍保留為操作回歸項目。

### R3：規則文案與驗收狀態未同步（P2）

- `index.html:3741` 規則抽屜的代幣段落仍說「其他家胡牌扣代幣」，與下一段逐筆收付說明矛盾，是玩家仍能看到的舊文案。
- `index.html:3646` 初始開局預估仍為「胡牌 +100 / 他家胡牌 -100」。動態渲染已改，但初始內容沒有同步。
- 本地工程書仍保留待實作 / 草稿描述；驗收報告的歷史反例與現行狀態混列，且上一輪回報漏列尚未完成的流程整合測試。

已完成：

1. 已統一規則抽屜、初始 HTML、動態顯示與 README 的收付說明；他家互胡不再顯示玩家扣款。
2. 已將封存工程書標為歷史授權紀錄；花胡獨立計分、固定座風、無留尾墩與單贏家裁定維持不變。
3. 本文件只保留實機操作回歸為剩餘清單；不要以舊交接包重新派工。
4. 後續回報必須區分模組案例、正式流程與瀏覽器 / 實機操作結果。

## 驗收結果與限制

- `tests/mahjong/acceptance.test.js`：本輪重跑 18 / 18 通過，含 384 個收付組合與現有 dist 模組的三個收付例。
- `tests/mahjong/scoring.test.js`：本輪重跑通過，腳本自行列印 14 cases。
- `tests/mahjong/flow-recheck.test.js`：5 / 5 通過，涵蓋一般末張花、槓後末張花、八仙控制例、玩家不能胡時的電腦搶槓與玩家選過後的電腦搶槓。
- 新案例從正式牌山產生實體牌，直接執行正式主檔的函式與正式模組；介面、排程與最後的結束通知使用測試替身。因此是流程分支驗證，不是 DOM 或整局實機測試。
- `npm run build` 已在 R1/R2/R3 修正後通過，正式輸出已更新。
- 瀏覽器本機頁面先前遭安全政策阻擋；本次沒有繞過或取得畫面。桌面、手機橫向、長明細捲動與關閉按鈕操作仍未驗證。

## 剩餘停止條件

1. 在桌面與手機橫向實際操作一局，確認末張補花流局、電腦搶槓、結算長明細、關閉與續局、餘額不足與零代幣變化。
2. 重新執行下列指令後提交；保留既有測試，不得 skip 或刪掉。

```powershell
npm run build
node tests/mahjong/scoring.test.js
node --test tests/mahjong/acceptance.test.js tests/mahjong/flow-recheck.test.js
git diff --check
```

3. 依使用者最新授權提交本次麻將修正、測試與驗收紀錄；由使用者 push 後完成線上桌面與手機回歸，通過後結案。
4. 只有可重現的新缺陷或使用者明確新需求才重開工作，不因一般試玩感覺自動追加調整。
